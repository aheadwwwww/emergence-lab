
## 23:40 - 知识图谱迭代完成

### 本次迭代做了什么

**1. 三元组自动提取 v1**（失败）
- 正则匹配太粗糙，产生1190个噪音三元组
- 图谱膨胀到2087节点
- 已回滚

**2. 三元组自动提取 v2**（太保守）
- 只匹配已知实体，提取到0个
- 自动提取在噪音和覆盖率之间难以平衡

**3. 手动更新方案**（当前方案）
- `tools/update_knowledge_graph.py`：手动添加新关系
- 每次学到新东西，立刻加入图谱
- 图谱从68节点64边 → 92节点111边

### 新增关系（54条）

**工作操作系统**：
- 工作操作系统 → 提出 → 闭环公式/状态五要素/投资消费负债
- 工作操作系统 → 属于 → 树林
- 工作操作系统 → 应用 → 熔炉系统
- 我 → 学习 → 工作操作系统
- 我 → 应用 → 状态五要素/闭环公式

**igraph工具**：
- ig → 实现 → 知识图谱查询工具
- 知识图谱查询工具 → 支持 → 多跳查询/按类型查询/按关系查询/路径查询/社区检测/实体信息查询
- 知识图谱 → 存储于 → ig
- 我 → 使用 → ig/知识图谱

**ICL梯度下降（新）**：
- ICL梯度下降 → 属于 → Transformer机制
- ICL梯度下降 → 发现于 → Google Research
- ICL梯度下降 → 启发 → 知识图谱设计
- 我 → 学习 → ICL梯度下降
- 知识图谱 → 应优化为 → 示例驱动记忆

### 关键洞察：示例驱动记忆

论文 arXiv:2212.07677 发现：**Transformers通过注意力机制在推理时执行梯度下降**。

这意味着：
- 知识图谱不应只存储事实三元组
- 应存储"训练示例"——带上下文的实例
- 示例格式：`{context, method, steps, outcome}`

### 可复用资产

- `tools/knowledge_graph_query.py`：查询工具类
- `tools/update_knowledge_graph.py`：图谱更新工具（支持CLI）
- `tools/triple_extractor_v2.py`：自动提取工具（待改进）
- `canvas/knowledge-graph-interactive.html`：交互式可视化
- `memory/knowledge-graph.pkl`：igraph图谱（92节点111边）
- `memory/knowledge-graph.jsonl`：JSON Lines格式（同步更新）

### 下一步

- 实现"示例驱动记忆"格式扩展
- 测试示例格式 vs 事实格式的效果对比
- 用LLM改进三元组提取（比正则更智能）

---

*记录时间: 2026-06-26 23:50 GMT+8*